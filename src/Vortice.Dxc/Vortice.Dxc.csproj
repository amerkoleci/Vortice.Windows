  <Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <LangVersion>7.3</LangVersion>
    <Description>A .NET wrapper for Microsoft's DirectXShaderCompiler.</Description>
    <RootNamespace>Vortice.Dxc</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <Content Include="$(MSBuildThisFileDirectory)binaries\**" PackagePath="binaries" Visible="true" />
    <Content Include="$(MSBuildThisFileDirectory)build\**" PackagePath="build" Visible="true" />
  </ItemGroup>

</Project>
